# This repo holds the RAW contract corpus only (source of truth).
# The retired Python/Mongo ingestion+pricing pass has been removed (see
# safaris-knowledgebase ADR-0001 / ADR-0003). Keep it out for good.

# Secrets — never commit
.env
.env.*

# Python (retired tooling)
__pycache__/
*.py[cod]
*_env/
venv/
requirements.txt

# Generated reports/artifacts
ingestion/reports/
